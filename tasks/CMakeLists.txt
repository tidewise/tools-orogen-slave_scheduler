# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(slave_schedulerTaskLib)
ADD_LIBRARY(${SLAVE_SCHEDULER_TASKLIB_NAME} SHARED 
    ${SLAVE_SCHEDULER_TASKLIB_SOURCES})
add_dependencies(${SLAVE_SCHEDULER_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${SLAVE_SCHEDULER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${SLAVE_SCHEDULER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SLAVE_SCHEDULER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SLAVE_SCHEDULER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SLAVE_SCHEDULER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SLAVE_SCHEDULER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SLAVE_SCHEDULER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SLAVE_SCHEDULER_TASKLIB_HEADERS}
    DESTINATION include/orocos/slave_scheduler)

